# HANDOFF.md — สถานะงานล่าสุด

> ใช้ไฟล์นี้ส่งต่องานข้ามเครื่อง (บ้าน ↔ ที่ทำงาน) — อ่านไฟล์นี้ก่อนเริ่ม session ถัดไป

**อัปเดตล่าสุด:** 2026-08-08

---

## สิ่งที่พี่ A ต้องทำเอง (ไม่ auto-sync)

รอบหน้าไปเครื่อง Office → รัน `git pull` ที่ `claude-config` (หรือรอ hook ใหม่ทำให้อัตโนมัติ) แล้ว copy `settings.json` ไปทับ `~/.claude/settings.json` อีกครั้ง (ครั้งเดียวพอ ตามที่เขียนไว้ใน README) — หลังจากนั้นทุกอย่าง sync ผ่าน `git pull` ปกติเลยค่ะ
