# Design Lab — ห้องเทียบหน้าตา UI ก่อนตัดสินใจ

> สร้าง 2569-08-28 · รอบการรื้อ design system ครั้งที่ 4
> **จุดประสงค์: ให้พี่ A เห็นของจริงบนหน้าจอก่อน แล้วค่อยเขียน spec — ไม่ใช่เขียน spec แล้วค่อยมารู้ทีหลังว่าไม่ชอบ**

---

## เปิดยังไง

```
เปิดไฟล์ design-lab/preview-kit.html ด้วยเบราว์เซอร์ (ดับเบิลคลิกได้เลย)
```

ไม่ต้อง build ไม่ต้องลง npm ไม่ต้องมีเซิร์ฟเวอร์ — ไฟล์เดียวจบ
(ต้องต่อเน็ตครั้งแรกเพื่อโหลดฟอนต์จาก Google Fonts ถ้าไม่มีเน็ตจะ fallback เป็นฟอนต์ระบบ หน้ายังใช้ดูได้ปกติ)

**ดูบนมือถือจริง:** ในหน้ามีกรอบมือถือ 390px ให้อยู่แล้ว (ขวามือ) แต่ถ้าอยากจับต้องจริงบนโทรศัพท์
บอกหนูให้ deploy ชั่วคราวด้วย skill `cloudflare-workers-deploy` แล้วเปิดจากมือถือได้

---

## มีอะไรให้ดูบ้าง

**แถบบนสุด** ปรับได้ 4 อย่าง — ค่าที่เลือกถูกจำไว้ใน `localStorage` ปิดแล้วเปิดใหม่ยังอยู่:

| ปุ่ม | ตัวเลือก |
|---|---|
| **ทิศทาง** | D1 Expressive · D2 Clarity · D3 Studio |
| **ธีม** | สว่าง · มืด · ตามระบบ (ครบ 3 สถานะ) |
| **ฟอนต์** | Anuphan · Noto Sans Thai · Sarabun · Bai Jamjuree · IBM Plex Sans Thai (ของเดิม ไว้เทียบ) |
| **ความแน่น** | แน่น · สบายตา |

**เนื้อหาที่ preview** — เป็นหน้าจอจำลองจริง ไม่ใช่ตารางสีเปล่าๆ:
1. **Dashboard** — topbar, sidebar, KPI 4 ใบ, กราฟเส้น, กราฟแท่ง, ตารางค่าเดินเครื่อง, chip สถานะ
2. **ฟอร์มบันทึกงาน** — input/select/textarea/date, สถานะ error, toggle, ปุ่ม 5 แบบ, toast 3 ระดับ
3. **รายการ + รายละเอียด** — list, กล่องยืนยัน, สถานะว่าง (empty), สถานะกำลังโหลด (skeleton)
4. **มือถือ 390px** — bottom nav, การ์ด 2 คอลัมน์, sidebar ยุบหาย

> ทั้ง 3 ทิศทางใช้ **HTML ชุดเดียวกัน 100%** (clone มาจาก `<template>` เดียว) ต่างกันแค่ค่า CSS variable
> จึงเทียบกันได้แฟร์ — ถ้าชอบตัวไหนแปลว่าชอบ "ดีไซน์" จริงๆ ไม่ใช่เพราะเนื้อหาต่างกัน

---

## 3 ทิศทางคืออะไร

| | **D1 · Expressive** | **D2 · Clarity** | **D3 · Studio** |
|---|---|---|---|
| อ้างอิงจาก | Material 3 Expressive (Google, Play Store) | Apple HIG / Liquid Glass (iOS 26) | เว็บทูลสมัยใหม่ (Linear · Notion · Vercel) |
| สี | เขียวมิ้นท์สด + เหลืองอำพัน tonal | เทาอ่อน grouped + ฟ้า iOS สด | neutral โทนอุ่น + เขียวมรกต |
| มุม | 18-30px (มนมาก) | 12-22px + ปุ่มแคปซูล | 9-16px (คมพอดี) |
| ความลึก | เงานุ่มไล่ระดับ | โปร่งแสงเบลอ (แถบบน/ล่าง) | เงาบางชั้นเดียว + เส้น hairline |
| จังหวะ | สปริง 0.34s | ลื่น 0.28s | เร็วสั้น 0.16s |
| เหมาะกับ | แอปที่อยากดูเป็นมิตร มีชีวิตชีวา | แอปที่อยากดูหรู พรีเมียม | เว็บทูลทีมงาน ข้อมูลเยอะแต่ไม่แข็ง |

---

## ให้ feedback ยังไง (สำคัญที่สุด)

บอกหนูเป็นข้อๆ ตามนี้ก็พอ — **ผสมข้ามทิศทางได้** เช่น *"เอาโครง D3 แต่สีของ D1 แล้วปุ่มมนแบบ D2"*

- [ ] ทิศทางไหนที่ **แรกเห็นแล้วรู้สึกดีที่สุด** (ไม่ต้องคิดเยอะ เอาความรู้สึกแรก)
- [ ] **สี** — ตัวไหนสว่าง/สดพอ ตัวไหนยังทึมอยู่
- [ ] **ความลึก** — ชอบเงานุ่ม (D1) เบลอโปร่ง (D2) หรือเงาบาง+เส้น (D3)
- [ ] **ความมนของมุม** — มนไป / คมไป / กำลังดี ที่ทิศทางไหน
- [ ] **ฟอนต์** — ลองสลับทั้ง 5 ตัวแล้วบอกว่าตัวไหนอ่านสบายตาที่สุด (นี่คือข้อที่ต้องลองด้วยตา อย่าเลือกจากชื่อ)
- [ ] **ความแน่น** — แน่น หรือ สบายตา
- [ ] **มืด** — ลองสลับธีมมืดทุกทิศทาง ตัวไหนสวยพอๆ กับสว่าง
- [ ] **มือถือ** — ดูในกรอบ 390px ตัวไหนใช้งานง่ายที่สุด
- [ ] อะไรที่ **ไม่ชอบเลย** ต้องตัดทิ้ง

พอพี่ A ตอบครบ หนูจะเขียน design system รอบที่ 4 จากของที่ผ่านตาจริงแล้ว
แล้วอัปเดตไฟล์ที่เกี่ยวข้องทั้งหมด (`design-system.md`, `layout-and-brand.md`, `tech-stack.md`,
`vibe-coding-core/SKILL.md`, `context-templates.md`, subagent, `USER.md`, `CLAUDE.md`, `HANDOFF.md`)

---

## ทำไมต้องมีห้องแล็บนี้ (ประวัติที่ห้ามลืม)

| รอบ | ระบบ | ผลลัพธ์ | เหตุผลที่ทิ้ง |
|---|---|---|---|
| 1 | **Neo-Glassmorphism** | ทิ้ง | — |
| 2 | **Tactile Plant UI** (2569-08 ต้นเดือน) | ทิ้ง | ดูซ้ำกันทุกแอป · contrast ต่ำ · มือถือไม่ดีพอ · ไม่มีเอกลักษณ์ |
| 3 | **Instrument Grade** (2569-08-27, commit `31d56af`) | ทิ้ง | สีทึม/หม่น · แบนไม่มีมิติ · ฟอนต์ IBM Plex Thai ไม่ถูกใจ · แข็งเป็นเครื่องมือเกินไป |
| 4 | *(กำลังเลือก)* | — | — |

**บทเรียนร่วมของ 3 รอบแรก:** ทุกรอบอนุมัติจาก **เอกสาร spec ที่เป็นตัวหนังสือ** ไม่เคยเห็นหน้าจอจริงครบก่อนตัดสินใจ
พอเอาไปสร้างแอปจริงถึงเพิ่งรู้ว่าไม่ชอบ → เสียเวลาไป 3 รอบเต็ม

**กติกาใหม่ตั้งแต่รอบนี้ไป:** จะแก้ design system ต้องแก้ที่ preview kit นี้ก่อน แล้วดูของจริง — ห้ามอนุมัติจากเอกสารเปล่าอีก

---

## หมายเหตุทางเทคนิค (เผื่อพี่ A อยากรู้ว่าโค้ดข้างในทำงานยังไง)

- **`data-dir` / `data-theme` / `data-font` / `data-density` บน `<html>`** — สลับทิศทางคือแค่เปลี่ยนค่า attribute
  แล้ว CSS จะหยิบชุด variable คนละชุดมาใช้ ไม่มีการโหลดหน้าใหม่หรือแตะ DOM เลย
- **Container query (`@container`)** — เป็นหัวใจที่ทำให้กรอบมือถือ 390px แสดง layout มือถือจริงได้
  โดยไม่ต้องย่อหน้าต่างเบราว์เซอร์ เพราะ responsive ผูกกับ *ความกว้างของกล่อง* ไม่ใช่ *ความกว้างของจอ*
  (ถ้าใช้ `@media` แบบเดิม กรอบมือถือจะโชว์ layout เดสก์ท็อปย่อส่วน ซึ่งหลอกตา)
- **ธีมครบ 3 สถานะ** — `:root` (สว่าง) · `@media (prefers-color-scheme: dark)` + `:not([data-theme="light"])` (ตามระบบ) ·
  `[data-theme="dark"]` (ผู้ใช้เลือกเอง) — ไม่ใช่รสนิยม แต่เป็นข้อบังคับ ไม่งั้นแอปจะเพี้ยนบนบางเครื่อง
- **`min-width: 0` บน grid item** — ค่าเริ่มต้นของ grid item คือ `auto` ทำให้ตารางที่กว้างเกินดันทั้งหน้าให้เลื่อนแนวนอน
  บั๊กนี้เจอตอนทดสอบจริงในกรอบมือถือ (`scrollWidth 538 > clientWidth 370`) แก้แล้วเหลือ 0
- **ไอคอนเป็น inline SVG sprite** — ไม่พึ่ง CDN เปิดไฟล์แบบ offline ก็ยังครบ
- **`localStorage` อยู่ใน `try/catch` เสมอ** — โหมดส่วนตัวของเบราว์เซอร์โยน error ได้ ถ้าไม่ดักหน้าจะพังทั้งหน้า
